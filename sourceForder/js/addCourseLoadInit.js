$(function(){

    // 更新数据
    //tableListHtml( tableHtml(tableJson) ) ;

    loadSCM(curr_school_index , curr_college_index ) ;

}) ;





































































